# ============================================
# DOCKER COMPOSE ENVIRONMENT VARIABLES
# ============================================
#
# This file is used by docker-compose.prod.yml
# Copy to .env on the production server:
#   cp .env.docker.example .env
#
# These are NOT Laravel variables - they're for Docker Compose only.
# Laravel variables go in .env.production
#
# ============================================

# ----------------------------------------
# PROJECT SETTINGS
# ----------------------------------------
# Used for container names: ${COMPOSE_PROJECT_NAME}_nginx, etc.
# Change this if running multiple instances of this app
COMPOSE_PROJECT_NAME=workouts

# ----------------------------------------
# DATABASE CREDENTIALS
# ----------------------------------------
# These are used to initialize MySQL container
# Must match what you put in .env.production for Laravel

DB_DATABASE=workouts_prod
DB_USERNAME=workouts

# REQUIRED - docker-compose will fail without these
# Use strong passwords in production!
DB_PASSWORD=testpassword123
DB_ROOT_PASSWORD=rootpassword123
